import { GalleryClient } from "@/components/gallery/gallery-client";

export function GeneralGallery() {
  return (
    <GalleryClient
      galleryType="general"
      title="General Gallery"
      subtitle="Fresh non-nude captures from the BareUnity community."
    />
  );
}
