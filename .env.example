DATABASE_URL=

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# PrivateEmail SMTP settings for the app-owned welcome + confirmation email.
# Registration uses Supabase Auth to generate the confirmation link, then sends
# that link through Nodemailer so members receive one combined signup email.
SMTP_HOST=mail.privateemail.com
SMTP_PORT=587
SMTP_USER=jeroen@bareunity.com
SMTP_PASS=
EMAIL_FROM="BareUnity <jeroen@bareunity.com>"
