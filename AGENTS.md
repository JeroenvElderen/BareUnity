# Codex working rules

For any frontend-visible change:

1. Run `npm run codex:screenshot` after the app is running.
2. Include the generated screenshot path in the final response.
3. If screenshot capture fails because of environment limits, mention the exact failure and command output.